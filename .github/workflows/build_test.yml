name: test

on:
  push:
    branches:    
      - master
  pull_request:
    branches:    
      - master

jobs:

  build:
    name: Build Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1

    - name: Build the stack
      run: docker-compose up -d
